description = "goblinframework-remote"

dependencies {
  compile(project(":goblin-transport"))
  compile(project(":goblin-registry"))

  testCompile(project(":goblin-test"))
  testCompile(project(":goblin-monitor"))
}
