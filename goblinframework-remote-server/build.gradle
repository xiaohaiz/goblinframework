description = "goblinframework-remote-server"

dependencies {
  compile(project(":goblinframework-remote"))
  compile(project(":goblinframework-transport-server"))

  testCompile(project(":goblinframework-test"))
}
