description = "goblin-embedded-jetty"

dependencies {
  compile(project(":goblinframework-embedded"))
  compile("org.eclipse.jetty:jetty-server:${jetty_version}")

  testCompile(project(":goblin-test"))
}
